# checking that comments work
# checking that a few common types of tokens are scanned correctly
# checking that indent, eol, and dedent are generated correctly
Hello
  42
    x = 99e-1
    y = 0x9a
      z = x + y
    1
24

# checking that tokens are separated correctly even when not separated by whitespace:
+self

# checking that indent/dedent/eol not generated between parens:
(
  1
    2
  3
)
  4
    5
  6
