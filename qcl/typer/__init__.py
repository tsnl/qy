from qcl import antlr

from . import context


def type_project(project):
    # TODO: iterate through each source module to seed a FreeVar
    #   (so imports will work)
    # TODO: iterate through each source module (again) to visit and type all symbols relative to each other.
    pass
