--- sub alphabet_soup[T, u] ---

a = "hello";
// x = T(0);
x = u;
z = 2;

--- sub modernism ---

a = 0x42;
another_x = alphabet_soup[Str, 42Q]:x;

--- sub postmodernism ---

a_re_examined = modernism:a;

--- sub functions ---

f1 = (x) -> {p = x; p};
f2 = (y) -> {y};
f3 = (z) -> {42f};

z = f3(2q);

w = f1(f2(z));

--- sub recursion ---

g ~ (F32) -> F32;
g = (x) -> {
    w = g(x);
    0f
};

--- sub linalg[T] ---

Vec2 = Struct {
    x ~ T;
    y ~ T;
};
Vec2i = linalg[I32]:Vec2;
Vec2f = linalg[F32]:Vec2;

--- sub hello ---

Vec2i = linalg:Vec2i;
Vec2f = linalg:Vec2f;

--- sub entry_point_for_v1_default_loader ---

main_for_v1_default_loader = () -> ML {0};
