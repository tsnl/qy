--- sub xyz[n] ---

n ~ I32;

number = n;

--- sub abc ---

abc = (x) -> TOT {
    a = 2*x + 1;
    b = 42^a;
    a + b
};
truth = 7*7 == xyz[abc(24)]:number;


--- sub entry_point_for_v1_default_loader ---

main_for_v1_default_loader = () -> ML {0};
