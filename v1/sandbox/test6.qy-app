--- sub test6 ---

Vector3f = Struct {
    x ~ F32;
    y ~ F32;
    z ~ F32;
};

new_vec3f = (x, y, z) -> TOT Vector3f {
    (x, y, z)
};

--- sub entry_point_for_v1_default_loader ---

main_for_v1_default_loader = () -> ML {0};
