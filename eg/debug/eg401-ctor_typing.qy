def main () = do {
    v = new Vec2F(2.0f, 3.0f);
    w = new Vec3F(2.0f, 3.0f, 4.0f);
    // return i32(v.x);
    return 0;
};

type Vec2F = (x: f32, y: f32);

// pub vec2f: (f32, f32) -> Vec2F;
// vec2f (a, b) = new Vec2F(a, b);

// x = vec2f(0.0f, 0.0f);
