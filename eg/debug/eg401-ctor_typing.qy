fn main () = do {
    v = new Vec2F(2.0f, 3.0f);
    w = new Vec3F(2.0f, 3.0f, 4.0f);
    // return i32(v.x);
    return 0;
};

type Vec2F = (x: f32, y: f32);

// fn vec2f (a: Float, b: Float): Vec2f = new Vec2f(a, b);
// val x = vec2f(0.0f, 0.0f);
