dm howdy {
    x := (v) -> (p) -> ST {
        # v--- violates the closure ban
        # do p <- v;

        do p <- 42f;
        @!p
    };
    y := (b) -> ML {
        x(0f)!(b)
    };
    x :: ClosureBan { F32 -> ![F32] -> ST F32 };
    y :: ClosureBan { ![F32] -> ML F32 };
};

# dm henlo {
#     q = (ip) -> {
#         @!ip
#     };
#
#     w = () -> ML {
#         x = make!(420l);
#         q(x)
#     };
#
#     q :: (![I64]) -> I64;
# };
