mod test3 {
    fake_predecessor = (x) -> TOT {x};

    fake_multiply = (i, j) -> TOT {j};

    factorial = (x) -> TOT {
    #    if (x == 0) {
        fake_multiply (x, fake_predecessor(x))
    #    } else {
    #        fake_predecessor(0)
    #    }
    };

    w = fake_multiply(0, 1f);
};